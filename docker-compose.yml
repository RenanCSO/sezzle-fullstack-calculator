services:
  backend:
    build: ./backend
    ports:
      - "5001:8080"  # 5001 avoids macOS AirPlay conflict on :5000

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
